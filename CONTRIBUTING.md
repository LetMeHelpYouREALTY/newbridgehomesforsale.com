# Contributing

## Setup

```bash
npm install
npm run dev
```

## Checks before PR

```bash
npm run type-check
npm run lint
npm run build
```

Push to a `cursor/*` feature branch and open a PR into `main`. Vercel previews deploy from PRs.
